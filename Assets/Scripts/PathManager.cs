using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using static statics;

public class RoadManager : MonoBehaviour
{
    [SerializeField] PATH Status = PATH.NONE;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
